// zm-socket: IPC and protocol layer
// Phase 2.1.A (MIN-D1) — CustomPaneBackend JSON-RPC types
// Phase 2.2 — Socket API (Unix domain socket + named pipe)
// Phase 3.3 — MCP server (rmcp)

pub mod rpc;
